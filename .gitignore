/xpanel
*.db
*.sqlite*
data/
*.test
*.out

# 含 JWT 密钥等敏感信息,首启自动生成,不入库
config.json
config.local.*

# harness 内部(worktrees 等)
.claude/

# release 构建产物
/release/

# 嵌入的前端真产物由 build-release.sh 生成,不入库;只保留占位 index.html
/web/dist/*
!/web/dist/index.html
